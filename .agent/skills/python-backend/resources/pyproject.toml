[tool.poetry]
name = "python-backend-ai"
version = "0.1.0"
description = "A premium Python backend for AI orchestration"
authors = ["Your Name <you@example.com>"]

[tool.poetry.dependencies]
python = "^3.11"
fastapi = "^0.110.0"
uvicorn = "^0.27.0"
python-dotenv = "^1.0.1"
langchain = "^0.1.0"
langgraph = ">=1.0.0"
langchain-openai = "^0.0.8"
pydantic = "^2.6.0"

[tool.poetry.group.dev.dependencies]
pytest = "^8.0.0"
ruff = "^0.3.0"
black = "^24.2.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
